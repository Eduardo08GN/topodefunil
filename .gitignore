node_modules/
.DS_Store
.playwright-mcp/
funil-organico/.ledger-v6.json
# ⭐ GLOB, nao um por agente. A lista nominal cobria 5 dos 12 ledgers: FLAGRANTE,
# NECROSE, PEE, VAZAMENTO, TROCA, ORGANICWAVE e COLO ficaram de fora, e bastava
# rodar um deles para o ledger virar arquivo solto a espera de entrar num commit
# por acidente. Agente novo passa a estar coberto no dia em que nasce.
# ⛔ O `.prisma-ledger.json` continua VERSIONADO de proposito (CLAUDE.md) — por
# isso a excecao logo abaixo.
funil-organico/.*-ledger.json
!funil-organico/.prisma-ledger.json
funil-organico/__pycache__/

# artefatos de build do PyInstaller
build/
dist/
*.spec
__pycache__/

# ⛔ Rascunhos de subagente. Em 2026-08-05 dois deles (_cha_patch.py,
# _tmp_cha_fix.py — copias de 141KB do cha_short) entraram no repo por um
# `git add -A` meu enquanto uma auditoria rodava, e derrubaram o gate de
# personagens: o medidor varre *_short.py e afins e passou a contar dois
# motores fantasma.
_*.py
*_tmp_*.py
